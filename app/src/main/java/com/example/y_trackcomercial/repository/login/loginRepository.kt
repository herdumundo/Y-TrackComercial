package com.example.y_trackcomercial.repository.login

import android.app.AlertDialog
import android.content.Context
import androidx.compose.foundation.layout.ColumnScope

fun mostrarAlerta(){
    /*val builder = AlertDialog.Builder(requireContext())
    builder.setTitle("Título del diálogo")
    builder.setMessage("Mensaje del diálogo")
    builder.setPositiveButton("Aceptar") { dialog, which ->
        // Acción a realizar al presionar el botón "Aceptar"
    }
    builder.setNegativeButton("Cancelar") { dialog, which ->
        // Acción a realizar al presionar el botón "Cancelar"
    }
    val dialog: AlertDialog = builder.create()
    dialog.show()
*/
}